I want you to help me design and build a simple marketplace web app.

Tech stack:
- Plain HTML, CSS with Tailwind via CDN (no build step)
- A single JavaScript file for logic: assets/js/app.js
- Static assets in assets/images (logo etc.)
- No frameworks (no React/Vue)

Branding and style:
- Use these brand colors:
  - Background: #faf8f3
  - Panel / cards: #fffdd4
  - Primary: #f68003
  - Primary hover: #f67800
- Use Tailwind classes that reference CSS variables:
  - bg-[var(--bg)]
  - bg-[var(--panel)]
  - bg-[var(--brand)]
  - hover:bg-[var(--brand-hover)]
- Global font: "Poppins", with system fallbacks.
- Look and feel: clean, modern, similar to a simple e-commerce marketplace.

File structure I want:

project/
  index.html          -> main marketplace page (listing)
  product.html        -> (optional step later) product details page
  cart.html           -> (optional step later) cart page
  assets/
    js/
      app.js         -> all frontend logic for index page
    images/
      logo.png       -> placeholder logo

For now, focus on index.html and app.js.

index.html requirements:
- Include Tailwind via CDN.
- Define CSS variables in :root for my colors:
  --bg: #faf8f3;
  --panel: #fffdd4;
  --brand: #f68003;
  --brand-hover: #f67800;
- Layout sections:
  1) Sticky header:
     - Left: logo image (assets/images/logo.png) and site name (e.g. “MarketPlace”).
     - Center (desktop only): search bar input with a 🔎 icon.
     - Right: nav links (“Products”, “Cart”, “Checkout”, “Sell”), with “Sell” using the brand primary button.
     - Mobile: a hamburger icon that toggles a simple mobile menu with the same links and a small search input.
  2) Hero section:
     - Left: headline like “Find great deals from trusted sellers”.
     - Subtext line.
     - Row of category pills (“All”, “Electronics”, “Home”, “Fashion”, “Sports”) using rounded Tailwind buttons.
     - Right: an illustration image (just use a placeholder like https://picsum.photos/seed/hero/720/420).
  3) Filters bar (under hero):
     - Sort dropdown with options: Recommended, Price: Low → High, Price: High → Low, Newest.
     - Min/Max price inputs with an “Apply” button.
  4) Product grid:
     - A responsive grid with id="products-grid".
     - A <template> element with id="product-card" that represents a single product card:
       - Image at top.
       - Title, short description, badge (e.g. “New” / “Sale”).
       - Price and simple rating (★).
       - “Add to Cart” button using primary colors, and a small “♡” wishlist button.
  5) Footer:
     - 3–4 simple columns (Marketplace, Help, Legal, Newsletter).
     - Newsletter input + button using brand colors.
     - Bottom copyright line with current year.

app.js requirements:
- Use plain JavaScript (no bundlers).
- Define a small helper function `$` for querySelector.
- Create a small in-memory product list (an array of objects) with fields like:
  - id, title, price, image, desc, badge, category, rating, createdAt
- Implement basic state:
  - search query
  - selected category
  - sort option
  - min/max price
- On page load:
  - Render some skeleton/placeholder cards while “loading”.
  - Then render product cards into #products-grid using the <template id="product-card">.
- Filtering behavior:
  - Filter by search (title and description).
  - Filter by category when category pills are clicked.
  - Filter by min/max price when the Apply button is clicked.
  - Sort by price asc/desc and newest.
- Add to cart behavior:
  - When “Add to cart” is clicked:
    - Store items in localStorage under key "cart".
    - If product already exists, increment qty; otherwise add with qty=1.
    - Temporarily change button text to “Added!” and revert back after a short delay.
- Mobile menu:
  - On small screens, clicking the hamburger button toggles the mobile menu div.

Extra constraints:
- Keep everything as simple and readable as possible.
- Use Tailwind utility classes, not custom CSS, except for the CSS variables and font family.
- I should be able to open index.html directly in the browser and see a working marketplace homepage with dummy products, filters, and functional “Add to cart” using localStorage.

Please:
1. Generate index.html and assets/js/app.js that match these requirements.
2. Make sure the code runs without errors when opened in a browser (no build tools).
